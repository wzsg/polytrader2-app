Place the bundled Node 24 runtime here before packaging.

Expected Windows layout:

resources/strategy-runtime/win32-x64/node.exe

The installer also copies isolated-vm and node-gyp-build into:

strategy-runtime/win32-x64/node_modules/
